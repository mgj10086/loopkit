# LoopCode Project Configuration
# See https://github.com/mgj/loopcode for documentation
version: "1"

settings:
  defaultBudget:
    maxTokens: 500000
    maxDurationMinutes: 30
    maxIterations: 10
  defaultMemory:
    store: filesystem
    path: .loopcode/state
  enforceMakerChecker: true

loops: {}
